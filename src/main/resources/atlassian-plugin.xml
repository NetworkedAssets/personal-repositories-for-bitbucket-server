<atlassian-plugin key="${project.groupId}.${project.artifactId}" name="${project.name}" plugins-version="2">
    <plugin-info>
        <description>${project.description}</description>
        <version>${project.version}</version>
        <vendor name="${project.organization.name}" url="${project.organization.url}" />
        <param name="plugin-icon">images/pluginIcon.png</param>
        <param name="plugin-logo">images/pluginLogo.png</param>
    </plugin-info>

    <!-- add our i18n resource -->
    <resource type="i18n" name="i18n" location="private-repositories-permissions-plugin"/>
    
    <!-- add our web resources -->
    <web-resource key="private-repositories-permissions-plugin-resources" name="private-repositories-permissions-plugin Web Resources">
        <dependency>com.atlassian.auiplugin:ajs</dependency>
        
        <resource type="download" name="private-repositories-permissions-plugin.css" location="/css/private-repositories-permissions-plugin.css"/>
        <resource type="download" name="private-repositories-permissions-plugin.js" location="/js/private-repositories-permissions-plugin.js"/>
        <resource type="download" name="images/" location="/images"/>

        <context>private-repositories-permissions-plugin</context>
    </web-resource>
    
    <!-- publish our component -->
    <component key="myPluginComponent" class="org.networkedassets.atlassian.stash.private_repositories_permissions.MyPluginComponentImpl" public="true">
        <interface>org.networkedassets.atlassian.stash.private_repositories_permissions.MyPluginComponent</interface>
    </component>
    
    <!-- import from the product container -->
    <component-import key="applicationProperties" interface="com.atlassian.sal.api.ApplicationProperties" />
    
</atlassian-plugin>